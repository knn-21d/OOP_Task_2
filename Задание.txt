1.

Добавить в класс Employee механизм присваивания сотрудникам идентификационных номеров (Каждый сотрудник получает уникальный номер, формула для его вычисления может быть любой. Например, идентификационный номер может быть равен порядковому номеру сотрудника. Сотрудник добавленный первым имеет id = 1, второй добавленный сотрудник имеет id = 2 и т.д.)

Для этого добавить в класс:
статическое поле int nextId;
поле id;

методы:

setId(),
getId(),
getNextId();

Внести необходимые изменения в конструкторы.

В качестве ответа напишите класс Employee, с внесенными изменениями.

2.

Создать класс Manager, подкласс класса Employee. Менеджер будет отличаться от сотрудника наличием поля бонус. Зар. плата менеджера вычисляется по формуле: зарплата + бонус.

Структура класса:

class Manager : Employee{

// поля:

// премия

 

//конструктор1:

// инициализировать переменные суперкласса с помощью параметризированного конструктора

// премии нет

 

//конструктор2:

// инициализировать переменные суперкласса с помощью параметризированного конструктора

// установить премию

 

// методы:

// возвращает значение премии

// устанавливает премию, размер премии передается в качестве параметра

// переопределить метод getSalary(), для менеджера метод должен возвращать сумму зарплаты и бонуса

// переопределить метод toString(), что бы он возвращал строку вида:
id менеджер ФИО зарплата(бонус)

//  переопределить метод toString() в классе Employee, что бы он возвращал строку вида:
id сотрудник ФИО зарплата

}

3. 

Внесите изменения в  графический пользовательский интерфейс для реализации:

- добавления менеджера или сотрудника;
- при добавлении сотрудника должно быть активно поле для ввода бонуса;
- при выводе информации отдельно отображать список сотрудников и отдельно списко менеджеров.

Файл со скриншотом формы прикрепите в качестве ответа.

4.

Создайте массив типа Employee и сохраните в него  3 объекта класса Employee и 3 объекта класса Manager, используя различные конструкторы (данные вводятся через графический пользовательский интерфейс), 
Выведите информацию о сотрудниках и менеджерах на форму. Для менеджеров необходимо вывести бонус.

Решение (код) введите в текстовое поле.